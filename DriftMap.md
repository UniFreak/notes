# Work

- gitlab snippet 
    - OAuth2.0
    - ~~Postman script~~

- snip
    - packaging
    - distribution
        + private pypi: `warehouse` || `devpi` || `pypiserver`
    - ~~reStructuredText vs markdown~~
    - subcommand: config
        + Click
    - config read & write
        + shelves
        + configparser