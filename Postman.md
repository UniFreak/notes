# Scripts

scripts are ran in `Postman Sandbox` - a JavaScript execution environment 

run order:

- collection pre-request
- folder pre-request
- request pre-request
- collection test
- folder test
- request test

api reference:

- `require(module)`: 
- ...